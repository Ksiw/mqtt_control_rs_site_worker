{
  "name": "site_worker",
  "version": "1.0.0",
  "description": "Cloud Hub & Reverse Proxy Worker for mqtt_control_rs",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "build": "tsc --noEmit",
    "check": "tsc --noEmit",
    "deploy": "wrangler deploy",
    "d1:local": "wrangler d1 migrations apply DB --local",
    "d1:remote": "wrangler d1 migrations apply DB --remote"
  },
  "dependencies": {
    "hono": "^4.7.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250224.0",
    "typescript": "^5.7.3",
    "wrangler": "^3.111.0"
  }
}
